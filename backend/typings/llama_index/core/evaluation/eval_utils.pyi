"""
This type stub file was generated by pyright.
"""

from typing import Any, Dict, List, Optional, TYPE_CHECKING, Tuple
from llama_index.core.base.base_query_engine import BaseQueryEngine
from llama_index.core.evaluation.base import EvaluationResult

"""
Get evaluation utils.

NOTE: These are beta functions, might change.

"""
if TYPE_CHECKING:
    ...
async def aget_responses(questions: List[str], query_engine: BaseQueryEngine, show_progress: bool = ...) -> List[str]:
    """Get responses."""
    ...

def get_responses(*args: Any, **kwargs: Any) -> List[str]:
    """
    Get responses.

    Sync version of aget_responses.

    """
    ...

def get_results_df(eval_results_list: List[Dict[str, List[EvaluationResult]]], names: List[str], metric_keys: List[str]) -> Any:
    """
    Get results df.

    Args:
        eval_results_list (List[Dict[str, List[EvaluationResult]]]):
            List of evaluation results.
        names (List[str]):
            Names of the evaluation results.
        metric_keys (List[str]):
            List of metric keys to get.

    """
    ...

def upload_eval_dataset(dataset_name: str, questions: Optional[List[str]] = ..., llama_dataset_id: Optional[str] = ..., project_name: str = ..., base_url: Optional[str] = ..., api_key: Optional[str] = ..., overwrite: bool = ..., append: bool = ...) -> str:
    """Upload questions to platform dataset."""
    ...

def upload_eval_results(project_name: str, app_name: str, results: Dict[str, List[EvaluationResult]]) -> None:
    """
    Upload the evaluation results to LlamaCloud.

    Args:
        project_name (str): The name of the project.
        app_name (str): The name of the app.
        results (Dict[str, List[EvaluationResult]]):
            The evaluation results, a mapping of metric name to a list of EvaluationResult objects.

    Examples:
        ```python
        from llama_index.core.evaluation.eval_utils import upload_eval_results

        result = evaluator.evaluate(...)
        upload_eval_results(
            project_name="my_project",
            app_name="my_app",
            results={"evaluator_name": [result]}
        )
        ```

    """
    ...

def default_parser(eval_response: str) -> Tuple[Optional[float], Optional[str]]:
    """
    Default parser function for evaluation response.

    Args:
        eval_response (str): The response string from the evaluation.

    Returns:
        Tuple[float, str]: A tuple containing the score as a float and the reasoning as a string.

    """
    ...

