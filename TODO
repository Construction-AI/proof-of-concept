1. Add a readme explaining how to form a .env file / remove the dotenv file from .gitignore for now (no credentials are stored - pure configuration)
2. Use values from .env file in llamaindex api
3. Add OpenAI support for an API key